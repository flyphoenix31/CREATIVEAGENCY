
<section class="actions">
    @include('portal.mail.partial.action')
</section>

<section class="filter">
    @include('portal.mail.partial.filter')
</section>

<section class="datalist">
    @include('portal.mail.partial.ajaxlist')
</section>

<section class="pm_modal">
    @include('portal.mail.modal.modal')
</section>

@include('portal.mail.script')
