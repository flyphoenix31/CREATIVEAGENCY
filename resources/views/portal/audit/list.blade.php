

<section class="filter">
    @include('portal.audit.partial.filter')
</section>

<section class="datalist">
    @include('portal.audit.partial.ajaxlist')
</section>

<section class="pm_modal">
    @include('portal.audit.modal.modal')
</section>

@include('portal.audit.script')
